{
  "include": ["src"], //경로 설정
  "compilerOptions": {
    "outDir": "build", //컴파일 위치
    "target": "ES6", //JS 컴파일 버젼 설정.
    "lib": ["ES6", "DOM"], // API의 환경설정.어떤 API를 사용하고 어떤 환경에서 코드를 실행하는 지를 지정
    "strict": true
  }
}
